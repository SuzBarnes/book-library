// const express = require('express');
// const bookController = require('../controllers/book');

// const router = express.Router();

// router.route('/').post(bookController.create);
// // router.route('/').get(bookController.read);

// // router.route('/:bookId').get(bookController.readById);
// // router.route('/:bookId').patch(bookController.update);
// // router.route('/:bookId').delete(bookController.delete);

// module.exports= router;